[package]
name = "storable-core"
version = "0.1.0"
edition = "2024"
description = "Core domain logic for Storable"
license = "MIT"

[dependencies]
thiserror = "2.0.17"
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }